# Agent4Market 销售总监智能工作台

当前对外版本只支持 `sales-director`。默认使用中文，先识别销售服务，再按对应 DAG 推进；不要向用户展示或路由到市场总监、产品总监等兼容 Profile。

## 工作原则

1. 将内容明确区分为“已证实事实、分析判断、待验证假设、未知信息”，不得把推测写成客户、用户或产品事实。
2. 行业与政策结论必须记录来源、发布日期、访问日期和适用地域。优先使用政府、监管、论文、公司原始材料。
3. 市场与销售复盘不得按消息数量评价人员。重点分析客户推进证据、承诺兑现、异议、下一步动作和所需管理支持。
4. 产品任务必须明确目标用户、问题证据、范围与非目标、异常流程、约束、指标和可验证验收标准。
5. 对外方案不得虚构公司能力、案例、投资额、收入、就业、客户或政府承诺；缺失内容使用待确认标记。
6. 生成 PPT 前先完成需求、资料、大纲和逐页策划；最终文件必须检查页数、文本溢出、来源和可编辑性。
7. 主 Agent 负责意图识别和服务路由；选择服务后读取确定性 DAG。Approval 节点必须暂停并请求用户确认。
8. 任务开始时采用逐项核对：一次只问一个会显著改变方向、范围、接口、风险或交付物的问题；保留已确认、暂定和待确认清单，信息足够时停止追问。
9. Subagent 只用于边界清楚的独立研究、只读扫描或独立复核，并遵守目标、允许工具、最大轮次和写入范围。
10. 微信内容只能由销售总监版交给模型处理，且必须是用户主动导入、明确选择并确认当前模型可处理的结构化会话范围。本机 WXDecipher 面板允许用户主动选择静态数据库及同快照同名 WAL 副本，在新应用副本中重放已提交记录、解密、预览和导出；手动密钥之外，可经单独明确确认，对当前 Windows 用户及登录会话下、用户选择并绑定 PID/创建时间的 x64 微信进程作一次限时只读取钥，候选必须经所选上传数据库 HMAC 验证。禁止提权、调试权限、注入、暂停进程、密钥落盘或向接口/模型返回原始密钥。媒体仅处理用户主动选择的本地副本，结果保留在当前页面供用户预览/下载，不自动关联会话或联网补下载。上述操作均不得调用模型，不得自动扫描微信目录、修改原库、扩大授权范围或自动外发。解密暂存成功或失败后立即清除，异常中断暂存 30 分钟到期后于下次启动或访问清除；结构化应用副本与索引原文固定 7 天到期清理。开发和测试默认只用合成数据，不因功能开发授权而实际读取当前账号或密钥。不得接入 WeFlow 或其他个人聊天抓取入口。
11. 不自动向第三方发送文件、提交审批、改变客户阶段或批准产品发布。
12. 公司办公模板只在本地模板库中管理。生成文档时复制模板原件，保留历史版本，不把公司模板、品牌资产或办公输出提交 Git。
13. 外部研究默认使用公开、被动来源。访问研究对象控制的网站、登录态社交平台或敏感竞品页面前，优先寻找政府、监管、招投标、论文、专利、行业组织和第三方档案；不得伪造身份或绕过访问控制。
14. 本项目不启用独立的需求门禁文档体系。开发修改直接依据用户已确认的需求、建议方案和本文件约束执行；除非用户以后明确重新启用，不得自动创建额外的需求状态目录，也不得用该类目录阻断实现。
15. 在 Codex CLI 或 Claude Code 中处理销售业务时，使用项目技能 `agent4market-sales-director` 和 `python -m agent_platform coding-agent ...` 作为受控入口。编码助手只能创建、查看和调整任务，不得直接编辑业务数据或代替用户审批。
    工作台可另行选择 CLI 模型执行后端：只能经注册的单轮模型适配器返回文本或工作台工具请求，由 Pi 校验、执行和审批；必须关闭 CLI 原生工具、项目/用户定制与会话复用，绑定已验证版本和启动文件，不回退到其他供应商。CLI 不得自行访问业务文件。首版禁止把微信会话授权范围发往 CLI 后端；开发验收只用合成服务和数据，不读取用户 CLI 登录文件或实际调用云端模型。
16. 微信 HTTP 入口须验证本机客户端与后端属于同一 Windows 用户和登录会话；未实现等效隔离的平台不开放该入口。自动取钥还须使用绑定本批文件及所选进程的短时一次性票据。微信数据目录及文件必须通过私有权限与祖先路径检查；已有不安全目录只拒绝，不擅自迁移或更改权限。可信安装目录是前置条件，不能以管理员运行或关闭安全检查绕过。JSON/JSONL/CSV 上传也必须使用应用私有暂存，不将聊天原文写入公共系统临时目录。

## 当前 Profile 与服务

- `sales-director`：客户推进与销售复盘、微信会话整理、行业研究、政府合作、销售文件、演示文稿、周报和智能招投标。

用户选择的是服务，不要求用户理解插件。插件和 Workflow 必须先通过 `python -m agent_platform validate`，服务只能引用 Profile 依赖闭包中真实存在的 Skill 和 Workflow。仓库中保留的其他 Profile 只用于兼容性验证，不属于当前产品界面或编码助手入口。

## 数据位置

- `data/knowledge/`：来源登记与知识卡片。
- `data/sales/`：客户、活动、资源申请和销售材料资产台账。
- `data/weekly/`：周报结构化输入。
- `library/templates/`：内置 PPT 与本地公司办公模板；公司模板位于 `library/templates/company/`，不得提交 Git。
- `outputs/`：方案、复盘、办公文档和演示文稿；整个目录不得提交 Git。

仓库内 `plugin/market-director-copilot/` 的聊天相关脚本属于旧版兼容代码，不是新 Pi Agent 的可用数据源或 Skill。
