{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://workflow-market.local/contracts/workflow.schema.json",
  "title": "Bounded DAG Workflow",
  "type": "object",
  "required": ["id", "plugin", "display_name", "entry_nodes", "output_nodes", "nodes"],
  "properties": {
    "id": {"type": "string"},
    "plugin": {"type": "string"},
    "display_name": {"type": "string"},
    "entry_nodes": {"type": "array", "items": {"type": "string"}, "minItems": 1},
    "output_nodes": {"type": "array", "items": {"type": "string"}, "minItems": 1},
    "nodes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "type", "depends_on", "permissions"],
        "properties": {
          "id": {"type": "string"},
          "type": {"enum": ["agent", "tool", "subagent", "approval", "parallel", "join", "validator"]},
          "depends_on": {"type": "array", "items": {"type": "string"}},
          "permissions": {"type": "array", "items": {"type": "string"}},
          "boundary": {
            "type": "object",
            "required": ["objective", "allowed_tools", "max_turns", "write_scope"]
          }
        },
        "additionalProperties": true
      }
    }
  },
  "additionalProperties": false
}
