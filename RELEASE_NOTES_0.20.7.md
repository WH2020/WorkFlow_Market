# Agent4Market v0.20.7

## 变更摘要

- 微信数据库支持按 Windows/macOS 平台自动检索，也可手动指定账号文件夹并查找下级目录。
- 增加专用数据目录授权与自选路径；创建前显示最终位置并确认，不修改微信原目录或父目录权限。
- 将数据库复制与解析拆成独立操作：先复制并校验整批 DB/WAL，再只解析专用目录中的副本。
- 解析失败可更正密钥或重新授权取钥后重试同一批副本，不再重新复制原库。
- 增加副本封存、主动清除、固定期限清理和清理失败重试；重复复制请求不会删除已封存副本。
- 保留已导入会话的去重、浏览、导出及原文到期清理机制。
- 修正共享配置父目录只读权限导致的误拒绝；项目目录、授权记录和数据副本仍严格私有，危险父目录仍拒绝。
- Windows 打包冒烟检查显式启用 Python 隔离参数，安装验收自动读取当前版本。

## 数据保留与限制

- 输入数据库副本在复制会话创建后 30 分钟到期，解析重试不延长时间；到期后在下次启动或访问时清理，也可主动清除。
- 每次解析产生的解密明文与转换暂存立即清理；清理失败时暂停再次解析并重试清理。密钥不保存，取钥授权不复用。
- 结构化副本与聊天索引原文固定保留 7 天；原微信数据库不被修改。
- 刷新或关闭页面后暂不支持恢复本批副本的操作入口；最多同时保留两批，建议离开页面前主动清除。
- 自选位置仍须通过私有权限与祖先路径检查，不申请管理员权限、不绕过安全校验、不自动迁移已有数据。
- 自动取钥仍为实验性兼容功能；开发验证仅使用合成数据，不代表所有真实微信版本均已验证。
- 操作应通过工作台串行流程执行；不支持同一批次绕过界面并发调用复制和封存接口。

## 验证

- 微信相关 Python 回归：150 项，其中 132 项通过、18 项按环境条件跳过。
- 微信前端单元测试：19 项通过。
- 目录权限与微信 HTTP 新回归：30 项通过；受控运行时测试 124 项通过，TypeScript 检查通过。
- JavaScript 语法与 Git 补丁空白检查通过。
- 真实浏览器及安装包验收状态以本次发布结果为准，不将单元测试等同于实机兼容验证。
